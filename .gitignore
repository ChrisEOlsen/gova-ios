# Xcode generated artifacts — use `xcodegen generate` inside ios/ to recreate
*.xcodeproj/
*.xcworkspace/
DerivedData/
xcuserdata/
*.ipa
*.dSYM.zip
*.dSYM
build/

# XcodeGen also generates the Info.plist from project.yml properties
GovaApp/Info.plist
ios/GovaApp/Info.plist

# OS
.DS_Store
.DS_Store?
._*

# Claude — generated per-developer
.claude/settings.local.json

# Build-execution scratch (task briefs, reports, review packages, ledger)
.gova-build/

# Personal notes
HELP.md

# Python (export transform test artifacts)
__pycache__/
*.pyc
