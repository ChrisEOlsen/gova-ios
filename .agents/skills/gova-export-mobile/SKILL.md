---
name: gova-export-mobile
description: Populate SEED.md's Generated Context from the linked gova-monolith app's api.json manifest. Use when the web app's API changed and the iOS side needs to catch up.
---

# GOVA iOS Export

The workflow is `.claude/commands/export-mobile.md`. Read that file completely,
then follow it step by step. It is the same transform Claude Code runs as
`/export:mobile`; this file exists because Codex reaches a workflow through a
skill directory and that one is a single command file.

Nothing about the workflow is restated here. Do not act on this file alone.
